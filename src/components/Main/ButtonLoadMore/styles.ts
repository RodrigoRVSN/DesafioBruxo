import { styled } from '@stitches/react'

export const Button = styled('button', {
  borderRadius: '0.5rem',
  cursor: 'pointer',
  fontSize: '2rem',
  padding: '0.5rem 2rem',
  width: 'fit-content'
})
